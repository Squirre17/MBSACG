
pub mod writer {
    use std::path::Path;
    use crate::state::State;
    pub fn pivot_inputs() {
        /* pivot seed file to formated name file */
        /* 具体来说，pivot_inputs 函数会首先选择一些输入文件作为“轴”，然后对其他所有输入文件进行变换，
        使它们与所选的轴文件相似，从而产生一组新的输入。这些新的输入数据将被添加到输入队列中，以便在后续测试中使用。 */
        unimplemented!()
    }
}